{
  "main": {
    "dialect": "postgresql",
    "host": "localhost",
    "port": 5432,
    "username": "postgres",
    "password": {
      "$filter": "env",
      "test-ci": "",
      "$default": "sukses123"
    },
    "db": {
      "$filter": "env",
      "test": "mastery-test",
      "$default": "mastery"
    },
    "sync": true,
    "forceSync": true,
    "debug": false,
    "logging": false
  }
}
