{
  "main": {
    "dialect": "postgresql",
    "host": "localhost",
    "port": 5432,
    "db": {
      "$filter": "env",
      "test": "mastery-test",
      "$default": "mastery"
    },
    "username": "postgres",
    "password": {
      "$filter": "env",
      "test-ci": "",
      "$default": "sukses123"
    },
    "sync": true,
    "forceSync": true,
    "debug": false,
    "logging": false
  }
}
